<% content_for :title, "#{@blog.title} - Fresh Stack" %>

<article class="blog-detail">
  <header class="blog-header">
    <h1><%= @blog.title %></h1>
    <div class="blog-actions">
      <button class="ai-summarize-btn" 
              data-url="<%= summarize_blog_path(@blog) %>"
              data-title="<%= @blog.title %>">
        <span class="magic-wand">🪄</span>
        Summarize with AI
      </button>
      
      <% if current_user == @blog.user %>
        <%= link_to "Edit", edit_blog_path(@blog), class: "btn btn-sm btn-outline" %>
        <%= link_to "Delete", @blog, data: { "turbo-method": :delete, "turbo-confirm": "Are you sure?" }, class: "btn btn-sm btn-danger" %>
      <% end %>
    </div>
  </header>

  <div class="blog-meta">
    <span class="author">
      by <%= link_to @blog.user.name, @blog.user %>
    </span>
    <span class="date">
      <%= @blog.created_at.strftime("%B %d, %Y at %I:%M %p") %>
    </span>
  </div>

  <div class="blog-content">
    <%= simple_format(@blog.body) %>
  </div>

  <%= render 'shared/image_gallery', images: @blog.images %>

  <div class="blog-tags">
    <% @blog.tags.each do |tag| %>
      <%= link_to tag.name, tag_path(tag.name), class: "tag-badge" %>
    <% end %>
  </div>

  <% if @blog.questions.any? %>
    <div class="related-questions">
      <h3>Related Questions</h3>
      <div class="questions-list">
        <% @blog.questions.each do |question| %>
          <div class="question-item">
            <h4><%= link_to question.title, question %></h4>
            <div class="question-stats">
              <span><%= pluralize(question.responses.count, "answer") %></span>
              <div class="vote-display compact">
                <div class="vote-item upvotes <%= 'zero' if question.upvotes_count == 0 %>">
                  <span class="vote-icon">👍</span>
                  <span class="vote-count"><%= question.upvotes_count %></span>
                </div>
                <div class="vote-item downvotes <%= 'zero' if question.downvotes_count == 0 %>">
                  <span class="vote-icon">👎</span>
                  <span class="vote-count"><%= question.downvotes_count %></span>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <div class="blog-navigation">
    <%= link_to "← Back to Blogs", blogs_path, class: "btn btn-secondary" %>
  </div>
</article>
